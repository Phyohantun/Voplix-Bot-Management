/** Free plan: max orders per UTC month (unless admin sets `override_free_orders_per_month`). */
export const FREE_ORDERS_PER_MONTH_DEFAULT = 20;

/** Broadcast sends in `broadcast_logs` — monthly cap (UTC) for Pro. */
export const BROADCASTS_PER_MONTH_PRO = 5;
/** Broadcast sends in `broadcast_logs` — monthly cap (UTC) for Plus. */
export const BROADCASTS_PER_MONTH_PLUS = 50;
